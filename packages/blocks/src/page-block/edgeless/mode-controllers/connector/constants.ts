export enum Direction {
  TOP = 'top',
  LEFT = 'left',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}

export enum ElementState {
  NONE = 'none',
  ALLOW_CONNECT = 'allow-connect',
  DISABLE_CONNECT = 'disable-connect',
}
