import { TextBlockModel, PageBlockModel } from '../../blocks/src';

export type { TextBlockProps } from '../../blocks/src';

// TODO support dynamic register
export const BlockMap = {
  TextBlockModel,
  PageBlockModel,
};
