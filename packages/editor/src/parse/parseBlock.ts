import { OpenBlockInfo } from "../clipboard/types";

export class ParseBlock {
    // todo
    public static block2Html(blocks: OpenBlockInfo[]): string {
        return '';
    }
    // todo
    public static block2Text(blocks: OpenBlockInfo[]): string {
        return '';
    }
}